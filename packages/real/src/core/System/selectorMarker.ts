import type { Component } from '@real/core/Component'

export const Read = (component: Component) => {}

export const Write = (component: Component) => {}

export const Not = (component: Component) => {}

export const ReadWrite = (component: Component) => {}
