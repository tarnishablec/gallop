import { component } from '@gallop/gallop'

import { Dock } from '@real/widgets/Dock'
import { Panel } from '@real/widgets/Panel'
import { Editor } from '@real/widgets/Editor'
import { Menu } from '@real/widgets/Menu'
import { Footer } from '@real/widgets/Footer'

component('re-dock', Dock)
component('re-panel', Panel)
component('re-editor', Editor)
component('re-menu', Menu)
component('re-footer', Footer)
