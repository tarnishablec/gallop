import { component } from '@gallop/gallop'

import { Panel } from '@real/components/Panel'
import { Editor } from '@real/components/Editor'
import { Dock } from '@real/components/Dock'

component('re-panel', Panel)
component('re-editor', Editor)
component('re-dock', Dock)
